# Don't update sibling workspace package.json files when `npm version` runs.
# This prevents semantic-release from triggering registry lookups for
# cross-package dependencies that haven't been published yet.
workspaces-update=false

# Treat peer-dep conflicts as warnings, not errors (npm v7+ default is strict).
legacy-peer-deps=true
